<div class="topnav">
  <%= link_to t('defaults.home'), top_path %>
  <%= link_to t('defaults.contact'), contact_path %>
  <%= link_to t('defaults.logout'), logout_path, data: { turbo_method: :delete }  %>
  <%= link_to t('defaults.mypage'), mypage_path(current_user) %>
</div>